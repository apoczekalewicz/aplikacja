apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: db-data
  namespace: aplikacja
  labels:
    app: db
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
